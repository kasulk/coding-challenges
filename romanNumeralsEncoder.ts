export function solution(number: number): string {
  // 1666 => M DC LX VI
  // create a map object
  //// break down the input number into its thousands, hundreds, tenth and ones
  // if the digit is greater than
}
